#
# This stack is deployed by applying an RKE2-native HelmChart custom resource (`helm.cattle.io/v1`).
# RKE2's helm-controller then installs/maintains the GPU Operator chart.
#
# Rationale: this matches the installation mechanism proven to work on RKE2/SLES.
#
defaultNamespace: kube-system

